apiVersion: apps/v1
kind: Deployment
metadata:
  name: kin2-workforce-app
  namespace: kin2-workforce
  labels:
    app: kin2-workforce
    component: webapp
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: kin2-workforce
      component: webapp
  template:
    metadata:
      labels:
        app: kin2-workforce
        component: webapp
    spec:
      containers:
      - name: app
        image: kin2-workforce:latest
        ports:
        - containerPort: 5000
        env:
        - name: NODE_ENV
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: kin2-workforce-secrets
              key: database-url
        - name: STRIPE_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: kin2-workforce-secrets
              key: stripe-secret-key
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              name: kin2-workforce-secrets
              key: session-secret
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: kin2-workforce-secrets
              key: openai-api-key
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: kin2-workforce-service
  namespace: kin2-workforce
spec:
  selector:
    app: kin2-workforce
    component: webapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP